services:
  webdav:
    image: bytemark/webdav
    container_name: webdav-server
    environment:
      AUTH_TYPE: Basic
      USERNAME: admin
      PASSWORD: admin
    ports:
      - "33333:80"
    volumes:
      - webdav-data:/var/lib/dav
    restart: unless-stopped

volumes:
  webdav-data:
